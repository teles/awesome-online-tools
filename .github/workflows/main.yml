on: [push]

jobs:
  parse_to_bookmarks:
    runs-on: ubuntu-latest
    name: Chrome bookmarks
    steps:
        - uses: teles/awesome-parser-action@v0.9
          with:
            input: "/README.md"
