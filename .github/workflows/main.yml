on: [push]

jobs:
  parse_to_bookmarks:
    runs-on: ubuntu-latest
    name: Chrome bookmarks
    steps:
        - uses: teles/awesome-parser-action@v0.8
          with:
            input: "/README.md"
